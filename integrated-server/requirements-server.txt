# Integrated DR screening server — Python dependencies.
# Python 3.11+ (developed against 3.11/3.13). Install:
#
#     python -m venv .venv
#     .venv/bin/pip install -r requirements-server.txt
#
# NOTE: `torch` here resolves to PyPI's default (CUDA-enabled) Linux wheel,
# which is large. For a slim CPU-only serving box use the official CPU index:
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# (Some networks block that CDN; PyPI's default wheel works everywhere.)

torch>=2.2
torchvision>=0.17
numpy
opencv-python-headless>=4.8
pillow>=10.0
flask>=3.0
reportlab>=4.0
